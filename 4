#include <iostream>
#include <string>
#include <sstream>
using namespace std;
int main()
{ 
    string im;
    string r;
    cin >> im >> r;
    for (int i = 0; i < im.length(); i++)
    {
        if (im[i] != '.' || im[i] != ' ')
        {cout << im[i];}
    }
    cout << '.' << r;
}
